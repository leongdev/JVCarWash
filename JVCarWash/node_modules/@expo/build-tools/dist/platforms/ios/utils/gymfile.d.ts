import { Credentials } from '../credentials';
interface GymfileOptions {
    nativeProjectDirectory: string;
    scheme: string;
    outputName: string;
    outputDirectory: string;
}
declare function createIfNotExists(credentials: Credentials, { nativeProjectDirectory, scheme, outputName, outputDirectory }: GymfileOptions): Promise<boolean>;
export { createIfNotExists };
