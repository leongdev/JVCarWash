import { BuilderCtx } from './context';
declare type Builder<T> = (ctx: BuilderCtx<T>) => Promise<BuildResult>;
interface BuildResult {
    projectDirectory: string;
    artifactPath: string;
}
export { Builder, BuildResult };
