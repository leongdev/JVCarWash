import { AppLookupParams } from '../api/IosApi';
import { Context, IView } from '../context';
export declare class SetupIosDist implements IView {
    private app;
    private nonInteractive;
    constructor(app: AppLookupParams, nonInteractive?: boolean);
    open(ctx: Context): Promise<IView | null>;
}
