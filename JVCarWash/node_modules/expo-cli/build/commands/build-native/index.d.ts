import { Command } from 'commander';
declare enum BuildPlatform {
    ANDROID = "android",
    IOS = "ios",
    ALL = "all"
}
interface BuildOptions {
    platform: BuildPlatform;
    skipCredentialsCheck?: boolean;
    wait?: boolean;
    profile: string;
}
export declare function buildAction(projectDir: string, options: BuildOptions): Promise<void>;
export default function (program: Command): void;
export {};
